services:
  backoffice:
    image: ghcr.io/amurseli/hornero-backoffice:latest
    container_name: hornero-backoffice
    ports:
      - "3001:80"
    networks:
      - hornero-network

networks:
  hornero-network:
    driver: bridge

services:
  ledger-service:
    build:
      context: ./ledger
    container_name: hornero-ledger-service
    env_file:
      - ./ledger/.env
    environment:
      SERVER_PORT: 8080
    ports:
      - "8080:8080"

spring.application.name=api-gateway
server.port=${SERVER_PORT:8080}
management.endpoints.web.exposure.include=health,info
ledger.client.base-url=${LEDGER_BASE_URL:http://ledger-service:8081}
ledger.client.transactions-path=${LEDGER_TRANSACTIONS_PATH:/internal/transactions}

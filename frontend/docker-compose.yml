services:
  frontend:
    image: ghcr.io/${GHCR_NAMESPACE:-proyectohornero}/hornero-frontend:latest
    build: .
    container_name: hornero-frontend
    ports:
      - "3000:80"
    networks:
      - hornero-network

networks:
  hornero-network:
    driver: bridge

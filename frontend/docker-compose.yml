services:
  frontend:
    image: ghcr.io/amurseli/hornero-frontend:latest
    container_name: hornero-frontend
    ports:
      - "3000:80"
    networks:
      - hornero-network

networks:
  hornero-network:
    driver: bridge
